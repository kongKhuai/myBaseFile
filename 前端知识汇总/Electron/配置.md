```
{
  "build": {
    "appId": "com.yourcompany.yourapp",
    "productName": "YourApp",
    "directories": {
      "output": "dist"
    },
    "files": [
      "dist/**/*", // 只打包必要的文件
      "node_modules/**/*", //之前的配置
      "!node_modules/**/*", // 排除所有node_modules
      "node_modules/{module1,module2}/**/*" // 只包含必要的模块
         "!node_modules/**/*",

      "node_modules/{@microsoft/fetch-event-source,axios,core-js,crypto-browserify,echarts,process,recorder-core,request,sass,sass-loader,segmentit,simple-keyboard,simple-keyboard-layouts,stream-browserify,vm-browserify,vue,vue-audio-visual,vue-markdown,vue-worker,worker-loader}/**/*"
    ],
    "electronVersion": "36.3.2",
    "publish": null, // 不发布到远程

    "win": {
      //"target": "nsis",
      "target": [
        {
          "target": "nsis",
          "arch": ["x64", "ia32"] // 可以根据需要只保留一种架构
        }
      ],
      "icon": "build/icons/icon.ico",
      "publisherName": "Your Company"
    },

    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    },
    "compression": "maximum", // 使用最大压缩
    "asar": true, // 使用ASAR打包
    "extraResources": [
      {
        "from": "path/to/resources",
        "to": "resources"
      }
    ],
    "nodeGypRebuild": false, // 避免不必要的重新编译
    "npmRebuild": false, // 避免不必要的重新编译
    "electronDownload": {
      "mirror": "https://npm.taobao.org/mirrors/electron/" // 使用国内镜像加速下载
    },
     // 移除mac和linux配置
     "mac": {
      "target": "dmg",
      "icon": "build/icons/icon.icns",
      "category": "public.app-category.productivity",
      "hardenedRuntime": true,
      "gatekeeperAssess": false,
      "entitlements": "build/entitlements.mac.plist",
      "entitlementsInherit": "build/entitlements.mac.plist"
    },
    "linux": {
      "target": "AppImage",
      "icon": "build/icons",
      "category": "Utility"
    },
    // 添加只生成安装包的配置
    "skipDir": true, // 不生成win-unpacked目录
    "extraMetadata": {
      "buildVersion": "1.0.0" // 可以设置版本号
    }
  }
}


```