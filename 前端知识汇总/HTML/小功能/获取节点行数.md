```
//       const pElement = document.getElementById("faultInfoType");

//       if (!pElement) return;

//       // 临时强制单行以获取单行高度

//       pElement.style.whiteSpace = "nowrap";

//       const singleLineHeight = pElement.clientHeight;

//       // 恢复自动换行

//       pElement.style.whiteSpace = "";

//       const totalHeight = pElement.clientHeight;

//       const renderedLines = Math.round(totalHeight / singleLineHeight);

//       console.log(`实际渲染行数：${renderedLines}`);
```